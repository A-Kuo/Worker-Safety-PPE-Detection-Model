# notebooks

Exploratory EDA and one-off analysis notebooks (e.g., the data distribution analysis in
Milestone M2). Kept out of the critical path — anything that needs to be reproducible
should be promoted to `scripts/` or `src/`.
