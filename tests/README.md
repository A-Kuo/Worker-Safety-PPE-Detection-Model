# tests

Planned home for unit tests, primarily for the label-remapping logic in `src/data` (the
riskiest, most error-prone part of this project per `docs/PROJECT_PLAN.md`'s risk
register) and the metric functions in `src/evaluation`. Not yet implemented.
