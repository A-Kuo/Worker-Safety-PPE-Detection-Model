# Draft unified dataset config (YOLO format). Mirrors src/data/label_schema.py -
# keep both in sync.
#
# STATUS: `scripts/build_unified_dataset.py` exists, is unit-tested, and has been
# smoke-tested end-to-end against one real source (docs/DATA_DISTRIBUTION.md).
# `path`/train/val/test below are still placeholders because the two Roboflow
# sources (personal-protective-equipment-combined-model, hard-hat-universe-0dy7t)
# are blocked on a Roboflow API key - see docs/PROJECT_PLAN.md Open Question #3.
# Run `python scripts/build_unified_dataset.py --source ... --out data/unified`
# once unblocked; that command also generates a real data.yaml at
# data/unified/data.yaml, which should be used for training instead of this file.
#
# `boots`/`no_boots` ARE confirmed available now: the Ultralytics Construction-PPE
# source (added this session, see docs/DATASET_NOTES.md #5) has both.

path: ../data/unified  # populated by scripts/build_unified_dataset.py (M2)
train: images/train
val: images/val
test: images/test

names:
  0: helmet
  1: no_helmet
  2: vest
  3: no_vest
  4: goggles
  5: no_goggles
  6: gloves
  7: no_gloves
  8: boots
  9: no_boots
  10: mask
  11: no_mask
  12: person
  13: machinery
  14: vehicle
  15: cone
  16: ladder
