# scripts

Planned home for `build_unified_dataset.py` (Milestone M2's reproducible merge
pipeline: export → dedup → remap → re-split) and other one-off/reproducible CLI
entrypoints. Not yet implemented — see `docs/PROJECT_PLAN.md` §4 (M2).
